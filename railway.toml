[build]
builder = "dockerfile"

[deploy]
startCommand = "/app/start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "never"

[[deploy.variables]]
name = "NODE_ENV"
value = "production"
